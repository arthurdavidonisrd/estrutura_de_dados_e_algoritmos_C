#include <stdio.h>
#include <stdlib.h>

int main()
{
    float salario, aumento;

    scanf("%f", &salario);

    if(salario <=){
        aumento = salario * 1.15;
        printf("O seu salario com aumento de 15% passa a valer %f", aumento);

    }
    else if(salario <=){
        aumento = salario * 1.10;
        printf("O seu salario com aumento de 10% passa a valer %f", aumento);

    }
    else{
        aumento = salario 1.05;
        printf("O seu salario com aumento de 5% passa a valer %f", aumento);
    }
    return 0;
}
